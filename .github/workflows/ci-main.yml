name: CI Main

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    uses: ./.github/workflows/reusable-build.yml
    with:
      push_image: true